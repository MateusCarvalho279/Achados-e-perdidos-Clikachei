# Sistema de Achados e Perdidos - Colégio COTEMIG
# Backend: Flask (Controller) + PyMySQL (acesso ao MySQL).
# Consultas com filtro/ordenação/JOIN/relatório vivem em stored procedures no
# banco (database/02_procedures.sql), chamadas pela camada Repository.
Flask>=3.0
PyMySQL>=1.1
